# File Tree: rag-engine

**Generated:** 5/4/2026, 12:32:05 PM
**Root Path:** `c:\Users\faqih\Documents\lernen\go\rag-engine`

```
├── cmd
│   ├── server          # HTTP API (Fiber)
│   └── worker          # Asynq worker (ingestion jobs)
├── internal
│   ├── config          # .env loading, config struct
│   ├── generation      # prompt build + LLM call
│   ├── ingestion       # parse -> chunk -> embed logic
│   ├── pipeline        # orchestrator (coordination between step)
│   ├── retrieval       # vector search + rerank
│   └── store           # DB layer (pgvector, documents, chunks)
├── migrations          # .sql files
├── pkg
│   └── provider
│       ├── embedder    # interface + Ollama impl
│       └── llm         # interface + Ollama impl
└── go.mod
```

---

_Generated by FileTree Pro Extension_
